# {{PROJECT_NAME}} (omit-design 项目)

AI 助手在本仓库工作的强制约定。

## 项目是什么

omit-design 项目 — 设计稿是真实可点击的 React 页面,不是图片。`design/` 下每个 `.tsx` 文件 = 一张稿子,自动注册到 `/designs/<group>/<name>` 路由。

技术栈:Vite + React 19 + TypeScript + Ionic React 8 + zustand + `@omit-design/preset-mobile`。

## 仓库结构

```
{{PROJECT_DIR}}/
├── app/             Vite 入口 + 路由(基本不改)
├── design/          ★ 业务设计稿(开发主战场)
├── mock/            ★ mock 数据(开发主战场)
├── preset/          (可选)项目级 token override / 私有组件
├── .claude/skills/  Claude 加载的 skill(自动)
├── eslint.config.js 三条硬规则(仅对 design/ 生效)
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 核心约束(违反 = 直接拒绝)

1. **业务页面(`design/**/*.tsx`)只能 import**:
   - `@omit-design/preset-mobile`(Om* 组件白名单)
   - `react` / `react-router` / `react-router-dom`
   - `ionicons/icons`(图标常量)
   - `@ionic/react`:仅 `IonList` / `IonBackButton` / `IonIcon`
   - 项目内相对路径(如 `../mock/foo`)

2. **业务页面禁止字面量**:颜色(`#hex` / `rgb` / 命名色)、像素长度。走 token(`var(--om-*)` / `var(--ion-*)`) 或 Om* 组件的 props。

3. **每个业务页面文件第一行**:`// @pattern: <name>`,name 必须存在为 `<project>/patterns/<name>/pattern.json` 的目录。Pattern 是项目本地资产 —— 新项目 `patterns/` 起步为空,通过下面的 skill 按需生成。

4. **声明的 pattern 必须真用其签名组件**:每个 `<project>/patterns/<name>/pattern.json` 的 `whitelist` 字段列出该 pattern 必须 import 至少一个的 `Om*` 组件。lint 由 `require-pattern-components` 强制。

5. **新增页面 / 模式**:优先走对应 skill:
   - 不知道做什么 → `/start`(状态诊断)
   - 有 PRD,想抽 pattern → `.claude/skills/distill-patterns-from-prd`
   - 无 PRD,想要新 pattern → `.claude/skills/add-pattern`(对话模式)
   - 新页面 → `.claude/skills/new-design`(空 `patterns/` 时会自动调上面两个之一)
   - 有 Figma 链接,想抽视觉主题 → `.claude/skills/bootstrap-from-figma`
   - 审查 → `.claude/skills/audit-design`
   - 准备发布 → `/ship-design <name>`(lint + a11y + 截图一气呵成)

## 编辑边界

`.claude/settings.json` 已配置 deny 规则,**默认禁止 AI 修改**:`app/`、`eslint.config.js`、`vite.config.ts`、`tsconfig.json`、`.husky/`、`package.json`。设计稿请只在 `design/` / `mock/` / `preset/` 下工作。如需改这些受保护文件,会触发权限确认。

## 提交保护

`npm install` 后已自动安装 git pre-commit hook(husky + lint-staged)。任何 `design/**/*.tsx` 改动在 `git commit` 时会跑 `omit-design lint`,失败则阻止 commit。

## 开发命令

```bash
npm run dev          # 启动 vite,默认 http://localhost:5173
npm run lint         # 合规检查(AI 友好结构化输出)
npm run build        # 生产构建
```

访问入口:
- `/workspace` — 项目工作台(项目列表 / Library / Theme Editor 都从这里进)
- `/designs/<group>/<name>` — 业务设计稿(`design/<group>/<name>.tsx` 自动注册)
- `/workspace/:projectId/theme-editor` — 主题编辑器

## 不要做的事

- ❌ 在业务页面写 `#FF6B00` 这种字面量
- ❌ 从 `@ionic/react` 直接 import `IonButton` 等视觉组件(走 Om* 封装)
- ❌ 接真实 API
- ❌ 在 `patterns/` 为空时直接写 `design/` 文件(必须先 distill-patterns-from-prd 或 add-pattern)
- ❌ 在 `mock/` 外写 mock 数据

## 何时问用户

- PRD 缺关键信息(pattern / 字段 / 主操作)
- 白名单缺组件(提议加封装而不是绕过)
- 要新增 pattern(全局影响)

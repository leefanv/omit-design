/**
 * 项目级 mock 数据。业务页面通过相对路径 import,例如:
 *
 *   import { brand } from "../mock/onboarding";
 *
 * 这里不受 ESLint 设计稿规则约束(只 design/ 内受约束),你可以放任何字段。
 */
export const brand = {
  tagline: "omit-design",
  version: "v0.1.0",
};
